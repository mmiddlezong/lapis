generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Deck {
  data Json?
  id   Int   @id @default(autoincrement())

  @@map("decks")
}
